import styled from 'styled-components'

import { ButtonComponent } from '@/components/button/styles'

export const ActionButtonContainer = styled(ButtonComponent)`
  width: 173px;
  height: 40px;
  font-size: 0.75rem;
  font-weight: 700;
`
